document.getElementById('formulario').addEventListener('submit', function(evento) {
    evento.preventDefault(); // vazio, só pra fazer o alert

    alert('Formulário enviado com sucesso!'); 
});
